import HGSLogo from './HGS_Logo.png'; // Adjust the path if needed

const ImageComponent = () => {
    return (
        <div>
            <img src={HGSLogo} alt="Logo" />
        </div>
    );
};

export default ImageComponent;